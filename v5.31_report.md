# SAFE DUCKY v5.31 업데이트 보고서

작성일: 2026-09-13
DB_VERSION: 39 (변경 없음 — 이번 업데이트는 새 데이터 필드를 추가하지 않았습니다)

이번 업데이트는 v5.30을 실기기에 설치한 뒤 보내주신 재제보 — "공유가 여전히 실패한다(NotAllowedError). '엑셀출력' 버튼으로 다운로드하라고 뜬다" — 를 해결하는 데 집중했습니다. 정확한 오류 이름을 알려주신 덕분에 근본 원인을 특정할 수 있었습니다. 함께 보내주신 새 아이콘 자료 2장(28개)도 이 앱의 실제 메뉴명과 정확히 일치하는 것들을 골라 적용했습니다.

---

## 1. 실제 구현 (코드에 반영되어 동작 확인됨)

### 1) 파일 공유 실패(NotAllowedError) — 구조적 원인 해결

**원인**: v5.30까지의 MIME 타입 재시도 조치는 "타입을 못 알아봐서 거부하는 경우"에는 도움이 되지만, 이번에 알려주신 `NotAllowedError`는 전혀 다른 원인입니다. 브라우저의 공유 기능(Web Share API)은 "사용자가 버튼을 누른 순간부터 약 5초"라는 짧은 시간 동안만 호출을 허용합니다. 위험성평가·작업허가서·TBM처럼 서식이 복잡한 엑셀(셀 색칠, 칸 병합, 사진 삽입 등)은 만드는 데 시간이 걸리는데, 이 시간이 길어지면 정작 파일이 다 만들어져서 공유를 호출하는 시점에는 이미 "방금 누른 게 아니다"라고 브라우저가 판단해 거부해버립니다. 반대로 돋보기 옆 텍스트 공유는 즉시 실행되기 때문에 이 문제가 없었던 것이고, 이는 처음부터 일관되게 관찰된 증상과 정확히 들어맞습니다.

**조치**: 공유 버튼을 누르면 우선 즉시 한 번 공유를 시도합니다(파일 생성이 빨랐다면 이전과 동일하게 한 번에 공유창이 뜹니다). 이 시도가 시간 초과로 거부되면, 화면 하단에 **"📤 지금 공유"** 배너가 나타납니다. 이 배너를 다시 한번 눌러주시면 — 그 누르는 행위 자체가 "방금 누른 것"이 되므로 — 파일 생성이 아무리 오래 걸렸어도 시간 제한 없이 공유창이 뜹니다. 즉 파일 공유가 느린 항목에서는 버튼을 두 번(공유 → 배너에서 한 번 더) 눌러야 할 수 있다는 뜻이며, 화면에 안내 배너가 명확히 뜨므로 사용자가 무엇을 해야 하는지 바로 알 수 있습니다.

이 방식은 브라우저 표준 동작 자체(사용자 활성화 만료)를 우회하는 유일하게 확실한 방법이라, 기기 성능이나 데이터 양과 무관하게 항상 동작합니다. 백업(JSON)·단일HTML 내보내기를 포함해 파일을 공유하는 모든 곳(6곳)에 동일하게 적용됩니다.

**정직하게 말씀드려야 할 부분**: 이번 원인 진단과 조치는 Web Share API 표준 문서가 명시하는 동작을 근거로 한 것이라 이전(MIME 재시도)보다 훨씬 확신을 가지고 있지만, 이 환경에는 실제 기기가 없어 **실기기에서 배너가 정상적으로 뜨고, 배너를 눌렀을 때 정말로 공유창이 뜨는지는 아직 확인하지 못했습니다.** 설치 후 위험성평가·작업허가·TBM 등에서 공유를 눌러 배너가 뜨는지, 배너를 누르면 카카오톡 등 공유창이 정상적으로 열리는지 꼭 확인해주세요. 혹시 이번에도 실패한다면 이번에는 배너를 누른 뒤에 뜨는 오류 이름을 알려주시면 됩니다.

### 2) 새 아이콘 자료(28개) 적용

보내주신 2장의 아이콘 시트를 분석한 결과, 이 앱의 안전모듈 메뉴 22개 중 20개와 이름이 정확히 1:1로 일치했습니다(예: "CAPA", "위험성평가", "작업허가 PTW", "법정선임" 등). 억지로 끼워 맞추지 않고, 정확히 일치하는 20개만 오려내어 다음 두 곳의 이모지를 실제 그림으로 교체했습니다.

- **홈 화면(타임라인) 상단의 바로가기 5개 버튼**: 사용자가 직접 순서를 정할 수 있는 메뉴 우선순위 상위 5개가 표시되는 자리로, 그림이 커서 가장 눈에 띄는 곳입니다.
- **안전모듈 화면의 "☰ 전체" 펼침 메뉴판**: 22개 메뉴를 그룹별로 보여주는 목록으로, 한 줄에 하나씩 충분한 공간이 있어 그림을 넣기 적합했습니다.

그림 파일을 불러오지 못하는 경우(파일 손상, 저장공간 문제 등)에는 자동으로 원래 이모지로 대체되도록 만들어, 최악의 경우에도 메뉴가 비어 보이거나 깨져 보이지 않습니다.

**적용하지 않은 부분과 이유**:
- 하단 탭바(타임라인/위험신고/안전모듈/대시보드/마이페이지)는 현재 선택 여부에 따라 색이 바뀌는 선(벡터) 아이콘을 쓰고 있는데, 이 방식과 고정된 색의 그림 아이콘을 섞으면 디자인이 통일되지 않아 보일 수 있어 그대로 두었습니다.
- 안전모듈 상단의 가로 스크롤 메뉴바(22개 항목이 한 줄에 나열되는 좁은 공간)는 이모지가 자리를 적게 차지해 항목을 최대한 많이 보여줄 수 있는데, 그림으로 바꾸면 한 화면에 보이는 항목 수가 줄어들 수 있어 이모지를 유지했습니다.
- "법규체크", "통합검색", "반복 위험요인" 3개 메뉴는 시트에 이름이 정확히 일치하는 그림이 없어(비슷한 개념의 "법정점검", "건강관리" 그림은 있었지만 실제 기능과 정확히 같지 않아 임의로 짝짓지 않았습니다) 이모지를 그대로 사용합니다.
- "타임라인", "위험신고", "안전모듈", "마이페이지", "대시보드", "점검등록" 그림은 하단 탭바용 벡터 아이콘 자리와 성격이 달라 이번에는 사용하지 않았습니다.

---

## 2. 확인이 필요한 내용

- **가장 중요**: 위험성평가·작업허가서·TBM 등에서 "공유" 버튼을 눌렀을 때 "📤 지금 공유" 배너가 뜨는지, 그 배너를 누르면 실제로 카카오톡 등 공유창이 열리는지 실기기에서 확인해주세요.
- 새로 적용한 아이콘 20개가 실제 화면에서 선명하게 보이는지(흐릿하거나 깨지지 않는지) 확인해주세요.

## 3. 미구현

- 안전모듈 상단 가로 스크롤 메뉴바와 하단 탭바에는 새 아이콘을 적용하지 않았습니다(위 이유 참고).
- "법규체크"·"통합검색"·"반복 위험요인" 메뉴, 그리고 "타임라인"·"위험신고"·"안전모듈"·"마이페이지"·"대시보드"·"점검등록" 그림은 이번에 사용하지 않았습니다.

---

## 데이터 안전성

이번 업데이트는 저장되는 데이터 구조를 전혀 바꾸지 않았습니다(DB_VERSION 39 유지, 새 migration 없음). 기존에 입력해두신 모든 기록은 그대로 유지됩니다.

## 테스트 범위

이 환경(샌드박스)에서 실제 진행한 테스트: 문법 오류 검사, 공유 재시도 로직의 4가지 시나리오(①즉시 성공 ②즉시 실패 후 배너에서 재시도 성공 ③배너에서 취소 ④배너에서도 실패) 자동화 테스트, 새 아이콘 20개가 실제로 로드되는지(브라우저가 각 이미지의 실제 픽셀 데이터를 정상적으로 읽어들이는지) 확인, 아이콘 로드 실패 시 이모지로 자동 대체되는지 확인, 안전모듈 22개 서브탭 전환 정상 동작, 콘솔 오류 0건.

실기기(안드로이드/iOS)에서의 공유 배너 실제 동작은 이 환경에서 확인할 수 없어 검증하지 못했습니다 — 이번에도 꼭 실사용 결과를 알려주세요.
