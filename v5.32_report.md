# SAFE DUCKY v5.32 업데이트 보고서

작성일: 2026-09-13
DB_VERSION: 39 (변경 없음 — 저장되는 데이터 구조는 건드리지 않았습니다)

---

## 0. 먼저 — v5.31의 진단이 틀렸습니다

v5.31에서 저는 원인을 "파일을 만드는 동안 사용자 동작(활성화)이 만료되는 것"으로 단정하고, "📤 지금 공유" 배너를 눌러 다시 시도하면 해결된다고 말씀드렸습니다. 그런데 배너를 눌러 **직접 재시도하신 뒤에도 같은 NotAllowedError**가 났습니다. 코드를 확인한 결과 v5.31에서 그 오류 문구가 나오는 곳은 "배너를 눌러 재시도했는데 실패한 경우" 단 한 곳뿐입니다. 즉 제 진단이 원인의 전부가 아니었다는 것이 분명합니다. 이번에는 추측 대신 표준 문서부터 확인했습니다.

**Web Share API 표준이 정의하는 NotAllowedError의 원인은 정확히 세 가지뿐입니다.**

| 원인 | 내용 | 이번 상황에서의 판단 |
|---|---|---|
| ① 권한 정책 차단 | `web-share` 권한 정책이 이 페이지의 공유를 막음 | 텍스트 공유는 성공하므로 가능성 낮음(단, 파일에만 다르게 적용될 여지 있음) |
| ② 사용자 동작 만료 | 버튼을 누른 시점의 활성화가 만료된 뒤 호출됨 | **아님** — 배너를 직접 누른 직후에도 실패했으므로 배제됨 |
| ③ 파일 형식 보안 차단 | 브라우저가 그 형식의 파일 공유를 보안상 허용하지 않음 | **가장 유력** |

그리고 결정적인 사실이 하나 있습니다. **`navigator.canShare()`는 ③을 검사하지 않습니다.** 그래서 "canShare는 된다고 답했는데 실제 share는 거부당하는" 상황이 표준상 정상적으로 성립합니다 — 지금 증상과 정확히 일치합니다.

문제는 브라우저가 셋 중 무엇인지 알려주지 않는다는 점입니다(오류 이름이 셋 다 똑같이 NotAllowedError입니다). 앱 코드만으로는 구분할 수 없고 실기기에서만 판별됩니다. 그래서 v5.32는 두 갈래로 대응했습니다.

---

## 1. 실제 구현 (코드에 반영되어 동작 확인됨)

### 1) 무엇이 원인이든 목적을 달성하게 — 공유 모달 신설

v5.31의 하단 배너는 "다시 한번 누르기"밖에 제공하지 못해서, 그래도 실패하면 오류 문구만 뜨고 막다른 길이었습니다. 이를 **공유 모달**로 대체해 재시도·실패 사유·대안·진단을 한 화면에 모았습니다.

- **📤 지금 공유하기** — 새 사용자 동작으로 재시도(②가 원인인 경우를 위해 유지)
- 실패하면 모달이 닫히지 않고 **그 자리에서 사유를 표시**합니다. 예: `공유 실패 (NotAllowedError) · v5.32` + 위 3가지 원인 중 ②는 아니라는 설명
- **📄 텍스트로 공유** — 파일 대신 **내용을 텍스트로** 보냅니다. 텍스트 공유는 이미 성공이 확인된 방식이라 이 경로는 확실히 동작합니다
- **💾 파일 다운로드** — 파일로 받아서 카카오톡의 "파일 첨부"로 보내는, 언제나 되는 방법
- **🔎 공유 기능 진단** — 아래 2)번

"텍스트로 공유"는 엑셀 워크북을 사람이 읽을 수 있는 텍스트로 바꿔서 보냅니다(`workbookToText`). 실제 작업허가서 데이터로 시험한 결과 예를 들어 이런 형태로 나갑니다.

```
■ 1_A동 배관 보수 화기작업
안전작업허가서 (Permit To Work)
허가번호 | PTW-2026-001 | 작업일자 | 2026-09-13
작업장소 | A동 배관 | 작업구분 | 화기작업
...
```

병합된 칸이 같은 값을 반복해서 "제목 | 제목 | 제목"으로 읽히던 것도 함께 정리했고, 너무 길어지면 2,500자에서 끊고 "이하 생략 — 전체 내용은 엑셀 파일에서 확인해주세요"를 붙입니다. 엑셀을 내보내는 6곳과 CSV 1곳 전부에 연결되어 있습니다. 다만 백업(JSON)·단일HTML은 내용을 텍스트로 보내는 것이 의미가 없어 이 버튼이 나오지 않고 다운로드만 제공합니다.

한 가지 분명히 해두겠습니다. 텍스트로 공유한 경우 앱은 **"파일을 공유했습니다"라고 말하지 않습니다.** "내용을 텍스트로 공유했습니다(파일이 아닌 요약 텍스트입니다)"라고 실제로 일어난 일만 표시합니다.

### 2) 원인을 확정하기 위한 "공유 기능 진단" 신설

마이페이지 맨 아래, 그리고 공유 실패 모달에서 들어갈 수 있습니다. **이것이 이번 업데이트에서 가장 부탁드리고 싶은 부분입니다.**

3개 버튼을 차례로 누르시면 이 기기에서 무엇이 막혀 있는지 판별됩니다(공유창이 뜨면 그냥 닫으셔도 됩니다 — 뜨는 것 자체가 성공입니다).

| 결과 | 의미 |
|---|---|
| 1) 텍스트 성공, 2) .txt 실패 | 이 기기·실행 방식에서 **파일 공유 자체가 막힘** |
| 1)·2) 성공, 3) .xlsx 실패 | **엑셀 형식만 보안상 차단됨**(원인 ③ 확정) |
| 1)부터 실패 | **권한 정책/주소 문제**(예: 앱을 인터넷 주소가 아닌 로컬 파일로 열고 있는 경우) |

화면 아래에는 실행 환경(실행 주소, 오리진, 보안 컨텍스트, 홈화면 설치 여부, canShare 결과 3종, 기기 정보)도 함께 표시됩니다. **이 화면을 캡처해서 보내주시면 원인이 확정되고, 그에 맞는 해결책을 바로 적용할 수 있습니다.** 예를 들어 ③으로 확정되면 엑셀 대신 허용되는 형식으로 내보내는 방식을, 주소 문제로 확정되면 호스팅(https) 적용을 진행하면 됩니다.

### 3) 함께 바로잡은 코드 문제

- **검사한 것과 실제로 보낸 것이 달랐던 문제**: v5.31까지는 `canShare({files})`로 검사한 뒤 `share({files, title, text})`로 보내고 있었습니다. 검사 대상과 전송 대상이 다르면 검사 결과를 믿을 수 없습니다. 표준 형태인 `{files:[file]}` 하나로 통일했습니다(v5.30에서 추측으로 넣었던 title/text 동봉은 제거 — 근거가 없었고, 일부 환경에서는 오히려 파일+텍스트 조합이 실패 원인이 됩니다).
- **CSV 공유 시 형식 문자열**: `text/csv;charset=utf-8;`처럼 파라미터가 붙은 값을 파일 형식으로 쓰고 있어 브라우저의 형식 대조에서 걸러질 수 있었습니다. 파일 형식은 `text/csv`로 정정했습니다(다운로드 동작은 그대로입니다).
- **백업 시각 누락**: 공유 모달에서 "파일 다운로드"를 고르면 백업 파일이 실제로 저장되는데도 "마지막 백업 시각"이 갱신되지 않았습니다. 다운로드도 백업으로 인정하도록 고쳤습니다.
- **오류 문구에 앱 버전 표시**: 이제 실패 문구에 `· v5.32`가 함께 나옵니다. 다음에 오류를 알려주실 때 어느 버전에서 난 문제인지 바로 알 수 있습니다.

---

## 2. 확인이 필요한 내용 (가장 중요)

1. **마이페이지 → 🔎 공유 기능 진단**에 들어가서 1→2→3을 차례로 누르고 **그 화면을 캡처해 보내주세요.** 이것만 있으면 원인이 확정됩니다.
2. 엑셀 공유가 실패할 때 뜨는 모달에서 **"📄 텍스트로 공유"가 정상 동작하는지** 확인해주세요. 이게 되면 최소한 내용 전달은 지금 바로 가능합니다.
3. 화면 하단 버전이 **v5.32**로 보이는지 확인해주세요(예전 화면이 캐시로 남아있지 않은지 확인용입니다).

## 3. 미구현 / 한계

- **파일 공유가 실기기에서 되도록 만드는 것은 아직 해결되지 않았습니다.** 원인 ③(형식 차단)이라면 엑셀을 다른 형식으로 바꿔 내보내는 작업이, 주소 문제라면 호스팅 적용이 필요한데, 어느 쪽인지 확정되기 전에 추측으로 큰 변경을 하지 않았습니다. 진단 결과를 주시면 바로 진행하겠습니다.
- 텍스트 공유는 **요약 텍스트**이지 공식 서식 파일이 아닙니다. 공식 제출용은 다운로드 후 파일 첨부를 이용하셔야 합니다.
- 백업(JSON)·단일HTML 내보내기에는 텍스트 공유 대안이 없습니다(내용 특성상 의미가 없어서입니다).

---

## 데이터 안전성

저장 데이터 구조는 전혀 변경하지 않았습니다(DB_VERSION 39 유지, 새 migration 없음). 기존 기록은 그대로 유지됩니다.

## 테스트 범위

이 환경에서 실제로 수행한 테스트: 전체 스크립트 구문 검사, 공유 흐름 6개 시나리오(①첫 시도 성공 ②첫 시도 실패 후 모달 재시도 성공 ③두 번 다 실패 후 텍스트 공유로 전달 ④다운로드로 전달 ⑤파일 공유 미지원 환경 ⑥취소) 자동화 검증, 실제 ExcelJS를 넣고 워크북→텍스트 변환(여러 시트·병합칸·날짜·서식문자 포함)과 2,500자 초과 시 잘림 동작 검증, 실제 작업허가서 내보내기를 통한 전 과정(엑셀 생성 → 파일 공유 2회 실패 → 텍스트 공유 성공) 검증, 공유 진단 3단계 기록 및 환경 정보 표시 검증, 공유 모달·진단 화면의 실제 렌더링을 휴대폰 폭(412px)에서 화면 캡처로 확인, 5개 탭·안전모듈 서브탭·메뉴 아이콘 회귀 확인, 콘솔 오류 0건.

확인하지 못한 것: 실제 안드로이드/iOS 기기에서의 공유 동작. 이 환경에는 실기기가 없고, 공유 기능은 코드로 흉내낸 테스트로는 실제 브라우저의 거부 여부까지 재현할 수 없습니다. 이것이 이번에 진단 기능을 넣은 이유입니다.

## 참고한 표준 문서

- Web Share API 명세 — https://w3c.github.io/web-share/
- MDN `Navigator.share()` — https://developer.mozilla.org/en-US/docs/Web/API/Navigator/share
