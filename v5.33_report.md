# SAFE DUCKY v5.33 업데이트 보고서

작성일: 2026-09-13
DB_VERSION: 39 (변경 없음)

---

## 1. 원인 확정

보내주신 진단 화면이 결정적이었습니다.

| 테스트 | 결과 |
|---|---|
| 1) 텍스트 공유 | 성공 |
| 2) .txt 파일 공유 | **성공** |
| 3) .xlsx 파일 공유 | **실패 — NotAllowedError (Permission denied)** |

실행 환경도 문제가 없었습니다(https 호스팅, 보안 컨텍스트 정상, iframe 아님, 홈화면 설치 상태, 안드로이드 Chrome 152). **텍스트도 되고 파일 공유도 되는데 엑셀만 안 되는 것**이므로, v5.32에서 정리한 세 가지 원인 중 **③ 파일 형식의 보안상 차단**이 확정됩니다.

크로미움 원본 코드에서 근거를 확인했습니다. `chrome/browser/webshare/share_service_impl.cc`의 `IsDangerousFilename()`은 아래 44개 확장자만 허용하고, 목록에 없으면 무조건 `PERMISSION_DENIED`(= NotAllowedError)를 반환합니다.

```
.avif .bmp .css .csv .ehtml .flac .gif .htm .html .ico .jfif .jpeg .jpg .m4a .m4v
.mp3 .mp4 .mpeg .mpg .oga .ogg .ogm .ogv .opus .pdf .pjp .pjpeg .png .shtm .shtml
.svg .svgz .text .tif .tiff .txt .wav .weba .webm .webp .xbm
```

| 이 앱이 내보내는 형식 | 허용 여부 |
|---|---|
| **.xlsx** (모든 공식 서식) | ❌ 목록에 없음 → 항상 거부 |
| **.json** (백업) | ❌ 목록에 없음 → 항상 거부 |
| .csv | ✅ 허용 |
| .html (단일 HTML 내보내기) | ✅ 허용 |

검사 기준이 MIME 타입이 아니라 **파일명 끝의 확장자**라는 점도 원본 코드 기준입니다. 그래서 지금까지 시도했던 MIME 타입 변경·재시도·사용자 재탭은 **원리적으로 통할 수 없었습니다.** 제가 v5.30과 v5.31에서 두 번 잘못 짚었던 이유이기도 합니다.

**결론: 엑셀 파일 자체를 카카오톡으로 "공유"하는 것은 크롬에서 불가능합니다.** 앱이 우회할 수 있는 문제가 아닙니다. 원본 엑셀 파일을 꼭 보내야 한다면 **다운로드 후 카카오톡의 "파일 첨부"** 를 쓰셔야 합니다. 이것은 제약이지 버그가 아니며, 앞으로도 바뀌지 않습니다.

---

## 2. 실제 구현 — 허용되는 형식으로 보내는 경로 신설

그래서 "엑셀을 그대로 보내기"가 아니라 **"허용되는 형식으로 바꿔서 보내기"** 를 구현했습니다. 이제 엑셀 공유가 막히면 공유 모달에 네 가지 선택지가 나옵니다.

| 버튼 | 내용 | 받는 사람이 보는 것 |
|---|---|---|
| **📊 CSV로 공유** | 워크북을 CSV로 변환(`workbookToCsv`). 병합된 칸은 좌상단에만 값을 두어 칸 배치 유지, 한글 깨짐 방지용 BOM 포함 | 엑셀에서 그대로 열림 |
| **📄 서식 그대로 공유** | 워크북을 HTML 문서로 변환(`workbookToHtml`). 셀 병합·배경색·굵기·정렬·열너비를 그대로 옮김 | 눌러서 바로 보임. 브라우저 인쇄에서 "PDF로 저장" 선택 가능 |
| **💬 텍스트로 공유** | v5.32의 요약 텍스트 | 카카오톡 대화창에 바로 읽힘 |
| **💾 원본 다운로드** | 원본 .xlsx 저장 | 파일 첨부로 보내면 진짜 엑셀 파일 전달 |

"서식 그대로 공유"는 실제 위험성평가서로 검증했습니다. 위험성 평가기준 매트릭스(색상 포함), 관리기준표, 평가표, 개선조치란까지 공식 서식 모양이 그대로 유지됩니다.

변환은 **실제로 그 버튼을 눌렀을 때만** 실행되므로(지연 생성), 평소 공유 속도에는 영향이 없습니다. 엑셀을 내보내는 5개 경로(위험성평가 전용 서식, 공식 서식 문서 일괄, 항목별 내보내기, 일반 목록 2종)에 한 번에 연결되어 있어 모든 모듈에서 동일하게 동작합니다.

### 함께 고친 것

- **실패가 확정된 버튼을 감춥니다.** 크로미움의 허용 확장자 목록을 앱에 상수로 넣어(`SHARE_PERMITTED_EXTENSIONS`), 허용되지 않는 확장자가 NotAllowedError로 실패하면 "지금 공유하기" 버튼을 숨기고 정확한 사유를 표시합니다. 될 리 없는 버튼을 계속 누르게 두지 않기 위해서입니다. iOS 등은 규칙이 다를 수 있어 **미리 차단하지는 않고** 항상 한 번은 시도합니다.
- **백업(.json) 공유도 같은 이유로 막혀 있었습니다.** 백업 파일은 내용이 그냥 텍스트라서, **공유할 때만 `.txt` 이름으로 보내도록** 했습니다(허용 확장자). 복원 화면의 파일 선택도 `.txt`를 받도록 넓혔고, 복원 로직은 원래 파일 내용을 읽어 해석하므로 확장자와 무관하게 동작합니다. 다운로드는 지금까지처럼 `.json` 그대로입니다.
- **진단에 4) .csv, 5) .html 테스트를 추가**했습니다. 이 두 형식이 실기기에서도 성공하는지 직접 확인하실 수 있습니다.

---

## 3. 확인이 필요한 내용

1. 엑셀 공유를 눌러 모달이 뜨면 **"📊 CSV로 공유"** 와 **"📄 서식 그대로 공유"** 가 실제로 카카오톡까지 가는지 확인해주세요.
2. 진단 화면의 **4) .csv, 5) .html** 테스트도 성공하는지 확인해주세요(이론상 허용 목록에 있어 성공해야 합니다).
3. 화면 하단 버전이 **v5.33**인지 확인해주세요.

## 4. 미구현 / 한계

- **원본 .xlsx 파일을 공유 시트로 보내는 것은 불가능합니다**(크롬 정책). 다운로드 + 파일 첨부가 유일한 방법입니다.
- HTML 변환에는 **엑셀에 삽입된 사진이 포함되지 않습니다**(표·서식만 옮깁니다). 사진이 필요한 경우 원본 다운로드를 이용하셔야 합니다.
- 앱 안에서 바로 **PDF 파일을 만들어 공유하는 기능은 넣지 않았습니다.** `.pdf`는 허용 목록에 있어 기술적으로는 가능하지만, 한글이 포함된 PDF를 앱에서 직접 만들려면 외부 라이브러리(약 600KB)를 추가로 불러와야 하고 서식이 깨질 위험이 있습니다. 우선 "서식 그대로 공유(HTML)" 후 받는 사람이 인쇄에서 PDF로 저장하는 방식으로 대체했습니다. 이 방식이 불편하시면 PDF 직접 생성을 다음 버전에서 검토하겠습니다.

---

## 데이터 안전성

저장 데이터 구조 변경 없음(DB_VERSION 39 유지). 기존 기록은 그대로입니다.

## 테스트 범위

실제로 수행한 테스트: 전체 스크립트 구문 검사, 허용 확장자 판별 함수 검증(xlsx·json 거부, csv·html·txt·pdf 허용, 대문자 확장자 포함), 실제 ExcelJS로 만든 위험성평가 워크북의 CSV 변환(BOM·병합칸 처리·45줄) 및 HTML 변환(colspan·rowspan·배경색·굵기 보존 확인), 변환된 HTML을 실제 브라우저로 렌더링해 공식 서식 재현 여부를 화면 캡처로 확인(빈 줄이 빈 칸으로 보이던 문제 발견·수정 후 재확인), 실제 위험성평가·작업허가서 내보내기를 통한 전 과정(xlsx 공유 시도 → 거부 → 모달이 사유 표시·재시도 버튼 숨김 → CSV/HTML 공유 성공) 검증, 백업 공유가 .txt 이름으로 나가는지와 복원 입력이 .txt를 받는지 확인, 공유 흐름 6개 시나리오 회귀, 공유 모달 렌더링을 휴대폰 폭(412px)에서 캡처 확인, 5개 탭·서브탭·메뉴 아이콘 회귀, 콘솔 오류 0건.

확인하지 못한 것: 실기기에서 CSV·HTML 공유가 실제로 카카오톡까지 전달되는지. 두 확장자 모두 크로미움 허용 목록에 있어 성공이 예상되지만, 실기기 확인 전까지 단정하지 않겠습니다.

## 참고

- Web Share API 명세 — https://w3c.github.io/web-share/
- 크로미움 `share_service_impl.cc` (`IsDangerousFilename()`의 허용 확장자 목록) — https://github.com/chromium/chromium/blob/main/chrome/browser/webshare/share_service_impl.cc
